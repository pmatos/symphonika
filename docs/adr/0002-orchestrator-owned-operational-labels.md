# Orchestrator-owned operational labels

Symphonika will let the orchestrator write a narrow set of GitHub operational labels for claim and runtime bookkeeping, while leaving workflow labels, comments, PR links, handoff labels, and closure to the coding agent and repository workflow. This preserves the Symphony boundary that product workflow policy lives with the agent, but adds durable GitHub-visible claims so process restarts do not accidentally duplicate issue runs.
