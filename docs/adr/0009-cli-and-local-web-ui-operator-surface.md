# CLI and local web UI operator surface

Symphonika will ship both a CLI and a local web UI/API from the first version. The CLI covers daemon startup, validation, project listing, run inspection, and stale-claim cleanup, while the local web surface makes multi-project runs, retries, normalized events, and raw provider logs practical to inspect during agentic debugging.
