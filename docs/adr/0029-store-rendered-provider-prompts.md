# Store rendered provider prompts

Symphonika will store the exact rendered prompt sent to each provider attempt, along with the autonomy preamble version, workflow contract path and content hash, provider name and command, workspace path, issue branch, and issue snapshot. Exact prompt capture is required evidence for debugging autonomous agent behavior and reproducing run context.
