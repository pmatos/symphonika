# Service config file name

Symphonika's default service config file is `symphonika.yml`. The name makes the file's owner explicit, avoids confusion with repository-owned `WORKFLOW.md`, and gives agents a stable default path to inspect when operating the local daemon.
