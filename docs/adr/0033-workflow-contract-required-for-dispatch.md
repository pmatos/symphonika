# Workflow contract required for dispatch

Symphonika requires each Project to have a valid repository-owned workflow contract before dispatch. The daemon must not silently fall back to a generic prompt because full-permission autonomous agents need explicit repository policy, handoff expectations, and validation instructions.
