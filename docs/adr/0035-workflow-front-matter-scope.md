# Workflow front matter scope

Symphonika workflow contracts may include optional YAML front matter for repository-owned execution preferences, but service discovery and Project configuration remain in `symphonika.yml`. Tracker settings, workspace roots, provider selection, and GitHub label filters belong to service config; workflow front matter is for prompt-adjacent execution policy.
